% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qmdTop20.r
\name{qmdTop20}
\alias{qmdTop20}
\title{Calculate QMD of Top 20 Percent of Tree records in a Plot or Stand}
\usage{
qmdTop20(
  data,
  stand = "StandID",
  dbh = "DBH",
  expf = "TPA",
  TPA = 0,
  CC = 0,
  sortData = F,
  debug = F
)
}
\arguments{
\item{data}{Data frame of tree records for a single stand or plot. Must
include stand/plot ID, DBH, and expansion factor (TPA) columns.}

\item{stand}{Character name of stand/plot ID column. Default is "StandID".}

\item{dbh}{Character name of DBH column. Default is "DBH".}

\item{expf}{Character name of expansion factor (TPA) column. Default is "TPA".}

\item{TPA}{Numeric stand-level trees per acre.}

\item{CC}{Numeric stand-level percent canopy cover corrected for overlap.}

\item{sortData}{Logical flag retained for compatibility; data are currently
sorted internally by descending DBH regardless of this argument.}

\item{debug}{Logical; if \code{TRUE}, prints debug output. Default is \code{FALSE}.}
}
\value{
Numeric QMD for the top 20 percent of plot/stand (minimum 20 TPA).
Returns \code{NA} if inputs are invalid or unavailable.
}
\description{
Calculates quadratic mean diameter (QMD) of top 20\% of tree records based on DBH in a
plot/stand assuming at least 20 TPA. This algorithm first determines if 20\% of
plot/stand TPA is larger than 20 TPA. The algorithm will then iterate across
a tree list sorted from largest to smallest DBH and sum (DBH^2 * expansion
factor) and expansion factors of the tree records until 20 TPA or 20\% of
plot/stand TPA (whichever is larger) is exceeded. QMD will then be calculated
from the squared diameter and TPA values. If the input tree list has 20 TPA or
less, then QMD is calculated from the entirety of the tree list. If percent
canopy cover of the stand is less than 10, then seedlings  (DBH == 0.1) are
included, otherwise they are ignored.
}
\examples{
df <- data.frame(
  StandID = c("S1", "S1", "S1"),
  DBH = c(16, 11, 6),
  TPA = c(12, 10, 15)
)
qmdTop20(data = df, TPA = sum(df$TPA), CC = 25)
}
