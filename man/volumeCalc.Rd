% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotAttr.r
\name{volumeCalc}
\alias{volumeCalc}
\title{Calculate Stand Volume Metrics}
\usage{
volumeCalc(
  data,
  stand = "StandID",
  dbh = "DBH",
  expf = "TPA",
  expfM = "MortPA",
  vol1 = "TCuFt",
  vol2 = "MCuFt",
  vol3 = "BdFt",
  vol1DBH = 0,
  vol2DBH = 5,
  vol3DBH = 9,
  debug = F
)
}
\arguments{
\item{data}{Data frame containing tree records from a single stand or plot.
Must contain stand/plot ID, DBH, expansion factors, and volume columns.}

\item{stand}{Character name of stand/plot ID column. Default is "StandID".}

\item{dbh}{Character name of DBH column. Default is "DBH".}

\item{expf}{Character name of live-tree expansion factor column. Default is "TPA".}

\item{expfM}{Character name of mortality expansion factor column.
Default is "MortPA".}

\item{vol1}{Character name of first volume column. Default is "TCuFt".}

\item{vol2}{Character name of second volume column. Default is "MCuFt".}

\item{vol3}{Character name of third volume column. Default is "BdFt".}

\item{vol1DBH}{Minimum DBH included in \code{VOL1} and \code{VOL4} calculations.
Default is 0.}

\item{vol2DBH}{Minimum DBH included in \code{VOL2} and \code{VOL5} calculations.
Default is 5.}

\item{vol3DBH}{Minimum DBH included in \code{VOL3} and \code{VOL6} calculations.
Default is 9.}

\item{debug}{Logical; if \code{TRUE}, prints debug output. Default is \code{FALSE}.}
}
\value{
Named numeric vector with:
\item{VOL1}{Live volume pool 1}
\item{VOL2}{Live volume pool 2}
\item{VOL3}{Live volume pool 3}
\item{VOL4}{Mortality volume pool 1}
\item{VOL5}{Mortality volume pool 2}
\item{VOL6}{Mortality volume pool 3}
}
\description{
Calculates six stand-level volume metrics from tree records. By default, the
function computes three live volume pools and three mortality volume pools
using DBH thresholds.
}
\examples{
df <- data.frame(
  StandID = c("S1", "S1"),
  DBH = c(8, 14),
  TPA = c(50, 30),
  MortPA = c(2, 1),
  TCuFt = c(5, 12),
  MCuFt = c(3, 9),
  BdFt = c(10, 35)
)
volumeCalc(df)
}
