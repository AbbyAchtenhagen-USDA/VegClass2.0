% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vegOut.r
\name{vegOut}
\alias{vegOut}
\title{Calculate Stand-Level Vegetation Output Variables}
\usage{
vegOut(
  data,
  region = 3,
  MPSGcovTyp = MPSGcovTyp,
  addHSS = addHSS,
  stand = "StandID",
  species = "SpeciesPLANTS",
  dbh = "DBH",
  ht = "Ht",
  expf = "TPA",
  crwidth = "CrWidth",
  vol1 = "MCuFt",
  vol2 = "SCuFt",
  vol3 = "SBdFt",
  vol1DBH = vol1DBH,
  vol2DBH = vol2DBH,
  vol3DBH = vol3DBH,
  InvDB = InvDB,
  InvStandTbl = InvStandTbl,
  customVars = customVars,
  customOutputFns = NULL,
  debug = F
)
}
\arguments{
\item{data}{Data frame containing tree records for a single stand or plot.
Must include stand ID, species, DBH, expansion factor, height, crown width,
and volume columns used by this function.}

\item{region}{Region identifier used to select classification rules. Valid
values are \code{1}, \code{2}, \code{3}, \code{8}, or \code{"MPSG"}. Default is \code{3}.}

\item{MPSGcovTyp}{Integer region code used in MPSG cover type calculations.
Used when \code{region = "MPSG"}.}

\item{addHSS}{Logical; if \code{TRUE}, include HSS outputs for MPSG runs.}

\item{stand}{Character name of stand/plot ID column in \code{data}.
Default is \code{"StandID"}.}

\item{species}{Character name of species (USDA plant symbol) column in
\code{data}. Default is \code{"SpeciesPLANTS"}.}

\item{dbh}{Character name of DBH column in \code{data}. Default is \code{"DBH"}.}

\item{ht}{Character name of height column in \code{data}. Default is \code{"Ht"}.}

\item{expf}{Character name of expansion factor (TPA) column in \code{data}.
Default is \code{"TPA"}.}

\item{crwidth}{Character name of crown width column in \code{data}.
Default is \code{"CrWidth"}.}

\item{vol1}{Character name of first volume column in \code{data}.
Default is \code{"MCuFt"}.}

\item{vol2}{Character name of second volume column in \code{data}.
Default is \code{"SCuFt"}.}

\item{vol3}{Character name of third volume column in \code{data}.
Default is \code{"SBdFt"}.}

\item{vol1DBH}{Minimum DBH threshold used for custom volume calculations
passed through to \code{customAttr()}.}

\item{vol2DBH}{Minimum DBH threshold used for custom volume calculations
passed through to \code{customAttr()}.}

\item{vol3DBH}{Minimum DBH threshold used for custom volume calculations
passed through to \code{customAttr()}.}

\item{InvDB}{Path to inventory database used by region-specific routines
(notably Region 1 and MPSG processing).}

\item{InvStandTbl}{Character name of inventory stand table in \code{InvDB}.}

\item{customVars}{Optional path to custom variable specification file.
If provided, custom attributes are appended to output.}

\item{debug}{Logical; if \code{TRUE}, prints debug output. Default is \code{FALSE}.}
}
\value{
Single-row data frame containing core stand metrics plus
region-specific classification variables and optional custom attributes.
}
\description{
Calculates a single-row output data frame of stand-level vegetation metrics
and region-specific classification variables from a tree list.
}
\details{
Core metrics include canopy cover, BA/TPA/QMD/SDI summaries for all trees
and stems, QMD of top 20 percent of TPA, and basal-area weighted diameter
and height. Additional outputs are appended according to the selected
regional rule set (\code{1}, \code{2}, \code{3}, \code{8}, or \code{"MPSG"}).
}
\examples{
\dontrun{
vegData <- vegOut(
  data = treelist_df,
  region = 3,
  stand = "StandID",
  species = "SpeciesPLANTS",
  dbh = "DBH",
  ht = "Ht",
  expf = "TPA",
  crwidth = "CrWidth",
  vol1 = "MCuFt",
  vol2 = "SCuFt",
  vol3 = "SBdFt",
  debug = FALSE
)
}
}
